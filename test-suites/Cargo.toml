[package]
name = "fee-vault-black-box-tests"
version = "0.1.0"
edition = "2021"
publish = false

[dependencies]
# Soroban 23.5.2's host test utilities require the rand_core 0.6 line.
# Keep this transitive dependency on the compatible release.
ed25519-dalek = "=2.2.0"
soroban-sdk = { version = "=23.5.2", features = ["testutils"] }
